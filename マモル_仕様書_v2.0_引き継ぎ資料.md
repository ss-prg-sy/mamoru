# マモル 仕様書・引き継ぎ資料

| 項目 | 内容 |
| --- | --- |
| アプリ名 | マモル |
| バージョン | v2.0（初回公開時点） |
| 更新日 | 2026-09-15 |
| 公開URL | https://mamoru-bbe60.web.app |
| リポジトリ | https://github.com/ss-prg-sy/mamoru （Private） |
| Firebase プロジェクト | mamoru-bbe60 |
| 状態 | 全機能の実装が完了し、公開済み |

---

## 0. この資料を読む方へ（AIアシスタント向けの前置き）

この資料は、マモルというアプリの**仕様と実装状況をまとめた引き継ぎ資料**である。別のセッションや別のアシスタントが作業を引き継げるように書いてある。

作業を始める前に、次の3点を押さえてほしい。

**1. 開発者はプログラミング初心者である。** 専門用語をそのまま出さず、何のための操作かを添えて説明すること。コードは「ここに貼ってください」と貼り先を明示した形で渡すのが確実。ファイル名だけ伝えても伝わらないことがある。

**2. 開発環境は GitHub Codespaces である。** ローカルにNode.jsは入っていない。ブラウザ上のVS Codeで作業する。ファイルの新規作成はエクスプローラーの右クリックよりも、ターミナルで `touch` を使うほうが事故が少ない。

**3. 仕様は開発者との対話で決めたものである。** この資料に書かれた判断（削除権限、上限値、セキュリティ方針など）は、すべて開発者が選んだ結果。勝手に変えず、変更が必要なら理由を添えて確認を取ること。

---

## 1. アプリの概要

### 何をするアプリか

3〜10人程度のグループで、やるべきことを出し合って共有するWebアプリ。合言葉ひとつで参加でき、アカウント登録は不要。

### なぜ作ったか

サークル合宿の準備のような短期的な共同作業で、既存の手段が不便だったため。

- **LINE**：タスクがメッセージに埋もれ、何が残っているか分からない
- **Notion / Trello**：機能は足りるが、登録と招待の手間が重く、全員に入れてもらえない

「合言葉ひとつで全員がすぐ入れて、やることが常に最新の状態で見える」ことに機能を絞っている。

### 名前の由来

期限を守る、約束を守る、から「マモル」。

### 想定利用シーン

サークル合宿の準備、旅行の幹事、イベント運営、家族の家事分担。数日〜数ヶ月の短期利用を想定。

---

## 2. 用語

| 用語 | 意味 |
| --- | --- |
| タスク欄 | タスクを共有する単位。1グループ＝1つ。**「ボード」とは呼ばない**（画面上の表記も統一済み） |
| 合言葉 | タスク欄に入るための共有パスワード |
| メンバー | 参加している人。アカウント登録はしない |
| 作成者 | そのタスクを追加した人 |
| 担当者 | そのタスクを頼まれた人。0人でも複数人でもよい |
| 管理者 | タスク欄を最初に作った人。一部の操作を独占する |

---

## 3. 確定した仕様

開発者との対話で決まった内容。**変更には確認が必要。**

### 3.1 参加と合言葉

| 項目 | 決定 |
| --- | --- |
| 参加方法 | 合言葉のみ。アカウント登録なし |
| 合言葉の長さ | 2文字以上10文字以内 |
| 大文字・小文字 | 区別する（`Camp` と `camp` は別物） |
| 一意性 | システム全体で一意。重複時はエラー |
| 保存 | 平文で `boards.passphrase` に保存。設定画面で表示するため |
| 照合 | SHA-256 のハッシュを `passphrases/{hash}` のドキュメントIDに使う |
| 合言葉の表示 | **伏せ字にしない。** 復元できない仕組みのため、忘れたときに確認できる必要がある |
| タスク欄の作成 | 参加画面の「新しくタスク欄を作る」から。合言葉の自動作成はしない |

### 3.2 本人の識別

名前ではなく**メンバーID（Firestoreの自動ID）で人を識別する**。

1. 初回参加時にメンバーIDを発行し、`localStorage` に保存
2. 2回目以降は合言葉も表示名も入力せずに復帰
3. 同姓同名でも別人として区別できる

端末が変わると新規参加扱いになるため、**同名のメンバーがいる場合は確認ダイアログ**を出す（「〇〇として続ける／別の人として参加する」）。前者を選ぶと既存メンバーIDを端末に紐づけ直す。

表示名は1〜12文字。タスク欄内での重複は許可する。

### 3.3 タスクの権限

ここが仕様の核心。**間違えやすいので注意。**

| 操作 | できる人 |
| --- | --- |
| 完了・未完了の切り替え（担当者がいるタスク） | 担当者と作成者 |
| 完了・未完了の切り替え（担当者なし＝みんな向け） | 誰でも |
| 編集（名前・詳細・期限・優先度・担当者） | **作成者のみ** |
| 削除 | **作成者のみ** |
| 並び替え | 誰でも |

判断の経緯：当初は「誤って追加したタスクを誰でも消せるように」という理由で削除を全員に開放していたが、最終的に編集・削除とも作成者限定に決まった。作成者が不在の間は誰も消せないが、**完了にすれば一覧から外れる**のでそれで足りるという判断。

削除の取り消しは、10秒→**5秒のトースト**に変更された。削除済み一覧の画面は作らない。

### 3.4 一覧の表示

- タブは設けない。**絞り込みボタン1つ**にまとめる
- 初期状態：未完了タスクが全部表示され、完了タスクは隠れている
- 絞り込める項目は**状態（未完了のみ／完了のみ／両方）と担当者（指定なし／自分あて／特定の人）の2つのみ**
- 絞り込み中はボタンが緑になり条件を表示、右の✕で一発解除
- 完了タスクに表示する日付は**完了日**（期限ではない）
- 期限が過ぎた未完了タスクは期限を赤字で強調
- 未完了の並び順は手動設定順、完了の並び順は完了日の新しい順

### 3.5 メンバーと管理者

| 操作 | できる人 |
| --- | --- |
| 自分の表示名の変更 | 本人 |
| 他のメンバーを外す | 管理者のみ |
| 合言葉の変更 | 管理者のみ |
| タスク欄の名前の変更 | 管理者のみ |
| タスク欄ごと削除 | 管理者のみ |

- 管理者が退出する場合、**引き継ぎ先を選ばないと退出できない**
- メンバーが自分1人なら引き継ぎなしで退出可能
- 外されたメンバーのタスクは残る。作成者の表示は「退出した人」になり、担当者からは外れる
- メンバーの削除は物理削除ではなく `isActive: false` にする論理削除

### 3.6 上限

| 項目 | 値 |
| --- | --- |
| メンバー | 10人 |
| タスク | 100件（未完了・完了の合計） |

**上限の理由は容量ではなく使い勝手。** Firestoreの無料枠（1GiB）に対して実際の使用量は桁違いに小さいので、容量の心配は不要。10人を超えるグループにはこのアプリが向かない、100件を超えると一覧として読めない、という判断による。

100件に達した状態で新規追加すると、**いちばん古い完了タスクが自動削除される**。完了タスクが1件もなければ追加を断って整理を促す。自動削除されたものは戻せない。

### 3.7 その他の決定

| 項目 | 決定 |
| --- | --- |
| 複数のタスク欄 | 1つの端末で使い分けできる。タスク欄名タップで切り替えシート |
| タスク欄の寿命 | 自動削除しない。利用者が消すまで残す |
| プッシュ通知 | 作らない（アプリを開いたときに見られれば十分という判断） |
| PWA（ホーム画面追加） | 対応しない（ブラウザで開ければ十分という判断） |
| リアルタイム同期 | あり。Firestore の `onSnapshot` を使用 |

---

## 4. 画面一覧と実装状況

全画面が実装済み。

| # | 画面 | ファイル | 備考 |
| --- | --- | --- | --- |
| 1 | 参加画面 | `JoinScreen.jsx` | 切り替えシートから来たときだけ戻るボタンが出る |
| 2 | タスク欄の作成 | `CreateBoardScreen.jsx` | 合言葉の確認入力あり |
| 3 | 表示名の入力 | `DisplayNameScreen.jsx` | 同名確認ダイアログを内包 |
| 4 | タスク一覧 | `TaskListScreen.jsx` | 絞り込み、⋮メニュー、切り替え導線 |
| 5 | 絞り込みシート | `FilterSheet.jsx` | 下から出るシート |
| 6 | タスク追加・編集 | `TaskFormScreen.jsx` | `task` propの有無で追加と編集を切り替え |
| 7 | タスク詳細 | `TaskDetailScreen.jsx` | 権限による出し分け、削除確認を内包 |
| 8 | 並び替え | `ReorderScreen.jsx` | dnd-kit によるドラッグ |
| 9 | メンバー | `MembersScreen.jsx` | 表示名変更・退出・引き継ぎ・メンバー削除を内包 |
| 10 | 設定 | `SettingsScreen.jsx` | 名前変更・合言葉変更・削除を内包 |
| 11 | タスク欄の切り替え | `BoardSwitchSheet.jsx` | 下から出るシート |
| 12 | 下部タブバー | `TabBar.jsx` | タスク／メンバー／設定 |

**注意：** 9と10は内部に複数の状態（`mode`）を持ち、1ファイルで複数画面を担当している。画面を足すときはこの構造に合わせるか、分割を検討すること。

---

## 5. データ構造（Firestore）

```
passphrases/{合言葉のSHA-256ハッシュ}
  boardId            対応するタスク欄のID

boards/{boardId}
  name               タスク欄の名前
  passphrase         合言葉（平文。設定画面で表示するため）
  ownerMemberId      管理者のメンバーID
  createdAt

boards/{boardId}/members/{memberId}
  displayName        表示名
  joinedAt
  isActive           false なら退出済み（表示から除外）

boards/{boardId}/tasks/{taskId}
  title              タスク名（1〜60文字）
  description        詳細（0〜500文字）
  dueDate            期限（"2026-09-20" の文字列。なければ null）
  priority           "high" | "medium" | "low"
  assigneeIds        担当者のメンバーIDの配列（空なら「みんな向け」）
  status             "open" | "done"
  orderIndex         並び順（数値。1000刻み）
  createdBy          作成者のメンバーID
  completedBy        完了者のメンバーID
  completedAt
  createdAt
  updatedAt
```

**設計上のポイント**

- `orderIndex` を1000刻みにしているのは、将来的に前後の中間値を挿入するだけで並び替えできるようにするため。ただし現状の `reorderTasks` は全件を振り直している
- タスクの削除は**物理削除**。「元に戻す」はクライアント側で5秒間データを保持し、同じIDで書き戻す方式（`restoreTask`）
- 合言葉の対応表を別コレクションにしているのは、`boards` を一覧取得せずに合言葉からタスク欄を引くため

---

## 6. ファイル構成

```
mamoru/
├── src/
│   ├── firebase.js              Firebase の初期化と接続設定
│   ├── App.jsx                  画面の切り替えとアプリ全体の状態
│   ├── App.css                  全画面のスタイル（約940行）
│   ├── lib/
│   │   ├── boards.js            タスク欄・メンバーの読み書き
│   │   ├── tasks.js             タスクの読み書き、権限判定、絞り込み
│   │   └── storage.js           localStorage（参加中のタスク欄の記憶）
│   └── screens/                 各画面（上の表を参照）
├── dist/                        ビルド結果（GitHubには上げない）
├── firebase.json                Hosting の設定
├── .firebaserc                  対象プロジェクトの指定
└── package.json
```

### 各ライブラリの役割

**`lib/boards.js`** — タスク欄とメンバーに関する処理。合言葉のハッシュ化、タスク欄の作成・取得、参加、メンバーの追加・削除・改名、管理者の引き継ぎ、合言葉の変更、タスク欄の削除。`MAX_MEMBERS = 10` もここ。

**`lib/tasks.js`** — タスクに関する処理。リアルタイム監視、追加・更新・削除・復元、並び替え、完了の切り替え。加えて**権限判定（`canComplete` / `canEdit`）と絞り込み（`applyFilter`）もここにある**。日付の整形関数もここ。`MAX_TASKS = 100` と `ensureCapacity` もここ。

**`lib/storage.js`** — `localStorage` の読み書き。どのタスク欄にどのメンバーIDで参加しているかを配列で持ち、最後に開いたタスク欄も記憶する。

**`App.jsx`** — 全画面の状態を持つ中心。`screen` の値で表示する画面を切り替える。タスクの監視、削除トースト、タスク欄の切り替えもここ。**画面を足すときはここに分岐を追加する。**

---

## 7. 技術構成

| 役割 | 使用しているもの |
| --- | --- |
| 画面 | React 19（Vite、JavaScript。TypeScriptではない） |
| データ | Firebase Firestore（リージョン asia-northeast1） |
| 公開 | Firebase Hosting |
| コード管理 | GitHub |
| ドラッグ操作 | @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/modifiers |
| 開発環境 | GitHub Codespaces |
| 料金プラン | Firebase Spark（無料）。カード未登録のため課金は発生しない |

CSSフレームワークは使っていない。`App.css` に手書きしたものを、クラス名で当てている。配色は緑（`#16a34a`）と白。CSS変数は `:root` にまとめてある。

---

## 8. 開発の再開手順

1. https://github.com/ss-prg-sy/mamoru を開く
2. 緑の **Code** ボタン → **Codespaces** タブ → 既存のものをクリック（なければ Create codespace on main）
3. ターミナルで `npm run dev`
4. 下の **ポート** タブで 5173 の行を右クリック → **ポートの表示範囲 → Public**
5. 「転送されたアドレス」のリンクから開く

**4を忘れると 404 や 401 になる。** Codespace を再開するたびにリセットされるので毎回必要。

### つまずきやすい点

- **404 が出たら、まずサーバーが動いているか確認する。** `Ctrl + C` を押すと止まる。ターミナルに `$` が出ていたら停止中
- サーバー起動中のターミナルには他のコマンドを打てない。右上の「＋」で別のターミナルを開く
- ファイルの新規作成はターミナルの `touch` が確実。エクスプローラーの右クリックだと、フォルダのつもりが同名のファイルになる事故が起きた
- 保存されているかの確認は `cat ファイル名` が確実。タブの白丸は当てにならないことがある

---

## 9. 公開の手順

コードを直したあと、この2つを打てば公開版が更新される。

```
npm run build
firebase deploy --only hosting
```

`npm run build` が `dist` フォルダに変換結果を作り、`firebase deploy` がそれをアップロードする。1分程度で反映される。

Firebase へのログインが切れていた場合は、先に次を実行する（Codespacesではブラウザが開けないので `--no-localhost` が必須）。

```
firebase login --no-localhost
```

---

## 10. セキュリティの現状

### 現在のルール（方針A）

```
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /passphrases/{hash} {
      allow get: if true;
      allow list: if false;
      allow create: if true;
      allow update: if false;
      allow delete: if true;
    }
    match /boards/{boardId} {
      allow get: if true;
      allow list: if false;
      allow create, update, delete: if true;
      match /members/{memberId} {
        allow read, write: if true;
      }
      match /tasks/{taskId} {
        allow read, write: if true;
      }
    }
  }
}
```

**考え方：** `list`（一覧の取得）を禁止することで、合言葉のハッシュ一覧やタスク欄の一覧を引き出せないようにしている。タスク欄のIDはFirestoreが発行するランダムな20文字なので、IDか合言葉を知らない限り中身にたどり着けない。

### 既知の限界

これは完全な防御ではない。**開発者もこの限界を理解したうえで選んでいる。**

- 合言葉やタスク欄IDを知っている人なら、ルール上は他人のタスクも書き換えられる。権限（作成者のみ編集など）は**画面上でボタンを隠しているだけ**で、ルールでは強制していない
- 合言葉の下限が2文字なので、短い合言葉は総当たりで破られうる
- 合言葉の入力失敗に対するレート制限は**未実装**

### 厳密にするなら（方針B）

Firebase Authentication の匿名認証を導入し、カスタムクレームに参加中のタスク欄IDを持たせて、ルール側で判定する。1〜2時間程度の作業になる。公開範囲が広がる場合や、大事な情報を扱うようになった場合は移行が必要。

---

## 11. 仕様書v1.0からの変更点

前回の仕様書と実装が食い違っている箇所。**実装が正しい。**

| # | 項目 | v1.0 | 現在 |
| --- | --- | --- | --- |
| 1 | 削除の権限 | 誰でも | **作成者のみ** |
| 2 | 編集の権限 | 誰でも | **作成者のみ** |
| 3 | 「元に戻す」の時間 | 10秒 | **5秒** |
| 4 | 削除の方式 | 論理削除＋30日保持 | **物理削除＋クライアント保持で復元** |
| 5 | 削除済み一覧 | 作る | **作らない** |
| 6 | 合言葉の表示 | 伏せ字 | **そのまま表示** |
| 7 | 並び替え画面の名称 | 優先順位を変更 | **並び替え** |
| 8 | 合言葉の保存 | ハッシュのみ | **平文も保存**（設定画面で表示するため） |

---

## 12. 未実装・今後の候補

### 仕様に書いたが実装していないもの

| 項目 | 状況 |
| --- | --- |
| 合言葉の入力失敗に対するレート制限 | 未実装。方針Bに移行する際に合わせて入れるのが自然 |
| オフライン時のバナー表示 | 未実装。Firestoreの再接続は自動で行われる |
| 楽観的更新とロールバック | 未実装。`onSnapshot` の反映速度で足りている |

### 使ってみてから判断するもの

- プッシュ通知（現状は「開いたときに見る」前提）
- PWA対応（ホーム画面からの起動）
- タスクの検索（100件までなので絞り込みで足りるはず）
- コメント機能（LINEを代替しない方針のため優先度は低い）

### UIデザインについて

画面デザインは別のAIに依頼して作成したモックがある。実装はそれを元にしているが、細部は完全には一致していない。**モックで未修正の指摘が1点残っている：** タスク詳細の「他の人が作成したタスク」のパターンで、編集ボタンが右上に残っている（実装側では非表示になっているので、モックの側を直す必要がある）。

---

## 13. 作業するときの心得

この開発で実際に問題になった点。

**仕様を勝手に埋めない。** 一度、決まっていない部分をこちらで判断して仕様書に書いたところ、開発者から「わかってないことがあるのに仕様書を書かないで」と指摘された。不明点は推測で埋めず、選択肢を示して確認を取ること。

**専門用語の言い換えを用意する。** 「ボード」が通じず、「タスク欄」に言い換えた経緯がある。Git と GitHub の違い、ローカルとホスティングの違いなども、その都度説明が必要だった。

**一度に渡す量を絞る。** ファイルを5〜6個ずつ、貼り先を明示して渡す形が機能した。まとめて渡すと、どこで失敗したか特定できなくなる。

**エラーはまず切り分ける。** 404や401はコードの誤りではなく、サーバーの停止やポート設定が原因だった。画面の見た目だけで判断せず、サーバーの状態を先に確認すること。
